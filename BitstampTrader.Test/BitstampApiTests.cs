using System;
using System.Threading.Tasks;
using BitstampTrader.Exchange;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace BitstampTrader.Test
{
    [TestClass]
    public class BitstampApiTests
    {
        //[TestMethod]
        //public async Task BitstampTicker()
        //{
        //    // arrange
        //    var bitstampExchange = new BitstampExchange();

        //    // act
        //    var ticker = await bitstampExchange.GetTickerAsync(BitstampExchange.BitstampTickerCode.BtcUsd);

        //    // assert
        //    Assert.IsNotNull(ticker.Last);
        //    Assert.IsTrue(ticker.Last > 0);
        //    Assert.IsTrue(ticker.Timestamp.Subtract(DateTime.Now).Minutes == 0);
        //}
    }
}
